import { Visualization } from './visualization';
import * as d3 from 'd3';

export class CalendarChart extends Visualization
{
    constructor(svgName: string, width: number, height: number)
    {
        super(svgName, width, height);
    }

    create(data) {

    }
}
